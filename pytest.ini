[pytest]
DJANGO_SETTINGS_MODULE = alugueldepobre.settings
python_files = test.py tests.py test_*.py tests_*.py *_test.py *_tests.py
addopts = 
    --doctest-modules
    --strict-markers
markers =
    slow: Run tests that are slow
    fast: Run fast tests
    functional_test: run tests that are selenium  based